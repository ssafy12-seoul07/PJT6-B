<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.pjt_B.mvc.model.dao.UserDao">
	<select id="selectOne" resultType="User" parameterType="Map">
		SELECT user_id, password
		FROM User
		WHERE user_id = #{id} AND password = #{password}
	</select>
	
	<select id="selectAll" resultType="User">
		SELECT *
		FROM User
	</select>
	
	<insert id="insertUser" parameterType="User" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO User (user_id, password, userName, nickName)
		VALUES (#{id}, #{password}, #{userName}, #{nickName});
	</insert>
</mapper>